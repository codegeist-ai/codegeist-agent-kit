---
description: Compatibility wrapper for the commit workflow
agent: build
---
Execute @.opencode/commands/commit.md.

Treat any user-provided context as additional commit-message guidance:
$ARGUMENTS

When you report the result, note that `/git-commit` is a compatibility alias for
the repo's `/commit` workflow.
